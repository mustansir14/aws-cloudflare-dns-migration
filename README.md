# AWS to Cloudflare (And Vice Versa) DNS Migration



## Installation

- Install dependencies using `pip install -r requirements.txt`

- Copy .env.example -> .env and fill in the required variables

## Usage


- `python main.py`